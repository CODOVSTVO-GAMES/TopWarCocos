export class salam {
    public type: string
    public level: number
    public index: number

    constructor(type: string, level: number, index: number) {
        this.type = type
        this.level = level
        this.index = index
    }
}